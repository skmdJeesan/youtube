// write a code to generate a random number between 0 to 9 (included)
function random0to9(){
    let x = Math.floor(Math.random()*10);
    return x;
}

//console.log(random0to9());

// write a code to generate a random number between 0 to 10 (included)
let num = Math.floor(Math.random()*11);
console.log(num);
// write a code to generate a random number between -10 to 10 (included)
let n = Math.floor(Math.random()*21-10);
console.log(n);